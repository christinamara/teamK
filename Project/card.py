class Card:
    number = 2
    suit = 1
    
    def make_card():
    #this is the constructor for card
    
    def getNumber():
        return number
    
    def getSuit():
        return suit
    
    def setNumber(i):
        number = i
        
    def setSuit(i):
        suit = i
    
    def equals(o):
        #check if this card is equal to object o which is another card
        #not sure if we need this, but it was in
        
    def compareTo(o):
        #compare to another card (used to check if same suit or number)